#include <iostream>
#include <string>
#include <vector>
using namespace std;
/*
void main()
{
	// 1. vector 사용하여 정수 저장 빈 벡터 선언
	
	vector<int> v(5);
	vector<int>::iterator it;

	// 2. 사용자로부터 5개 정수 입력 및 추가
	cout << "5개의 정수를 입력해주세요" << endl;

	for (int i = 0; i < size(v); i++)
	{
		cin >> v[i];
	}

	// 3. 벡터 크기 출력
	cout << "벡터의 크기는 " << size(v) << " 입니다." << endl;
	cout << "----------------------" << endl;
	// 4. 벡터 모든 원소 출력, 5. 가장 큰 값 출력
	
	int max = INT32_MIN;
	cout << "벡터의 모든 원소 출력" << endl;
	for (int i : v)
	{
		cout << i << " ";
		if (i > max) { max = i; }
	}
	cout << endl;
	cout << "벡터 내의 가장 큰 값은 " << max << " 입니다." << endl;
	cout << "----------------------" << endl;
	// 6. 벡터 모든 원소 두배로 늘리기
	
	for (int i = 0; i < size(v); i++)
	{
		v[i] *= 2;
	}

	cout << "벡터의 값을 2배로 늘린 결과" << endl;
	for (int i : v)
	{
		cout << i << " " << endl;
	}
	cout << "----------------------" << endl;
	// 7. 인덱스 입력 받아 해당하는 인덱스의 값 제거
	it = v.begin();

	cout << "제거할 값의 인덱스를 입력해주세요." << endl;
	int idx;
	cin >> idx;

	v.erase(it + idx);
	cout << idx << "번째 값 삭제 결과" << endl;
	for (int i : v)
	{
		cout << i << " " << endl;
	}
	cout << "----------------------" << endl;
	// 8. 인덱스와 값을 받아 해당 인덱스에 값 추가
	int inputIdx;
	int inputValue;
	cout << "추가할 인덱스와 값을 입력해 주세요.\n" << "인덱스 : ";
	cin >> inputIdx;
	cout << " 값 : ";
	cin >> inputValue;

	v.insert((it + inputIdx), inputValue);
	cout << inputIdx << "번째에 " << " 값 " << inputValue << "추가 결과" << endl;
	for (int i : v)
	{
		cout << i << " " << endl;
	}
}
*/
